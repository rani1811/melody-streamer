apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: music-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: raniingale2025/music-frontend:latest
          ports:
            - containerPort: 80
