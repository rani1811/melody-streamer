FROM jenkins/jenkins:lts

USER root

# Install Docker CLI only (not full daemon)
RUN apt-get update && \
    apt-get install -y docker.io && \
    apt-get clean

# Add jenkins user to docker group
RUN groupadd -f docker && usermod -aG docker jenkins

USER jenkins
